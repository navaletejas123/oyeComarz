/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["oyecommerz.com", "images.pexels.com", "via.placeholder.com","oyelabs.com"],
  },
};

module.exports = nextConfig;
